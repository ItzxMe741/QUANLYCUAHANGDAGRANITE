@model IEnumerable<CUAHANGBANDAGRANITE.Models.Trangthai>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Matt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tentt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Active)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Matt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tentt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Active)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Idtt">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Idtt">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Idtt">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
